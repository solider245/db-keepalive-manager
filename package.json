{
  "name": "db-keepalive-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
